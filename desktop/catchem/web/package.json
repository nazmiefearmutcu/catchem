{
  "name": "catchem-web-shim",
  "private": true,
  "version": "0.1.0",
  "description": "Catchem boot-shim. The real UI is served by the FastAPI sidecar — this only exists to satisfy Tauri's frontendDist requirement for the dev startup hand-off page.",
  "scripts": {
    "dev": "vite --port 1421",
    "build": "vite build",
    "preview": "vite preview --port 1421"
  },
  "devDependencies": {
    "vite": "^5.4.8"
  }
}
